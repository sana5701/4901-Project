Still needs:
    - pipreqs isn't working still, need to create requirements.txt
    - hook up onClick() button to a fetch() function for backend
    - Host frontend and backend to communicate
    - refactor backend code to communicate with new AI features